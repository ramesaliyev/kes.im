import {describe, it, expect} from 'vitest';

/**
 * Test generated banneds list.
 */
describe('site', () => {
  it('should has alive test setup', async () => {
    expect(true).toBe(true);
  });
});
