{
  "type": "module",
  "scripts": {
    "// Development": "",
    "dev": "wrangler dev --env local --port 3000 --local-protocol=https",
    "dev:remote": "wrangler dev --env local --port 3000 --local-protocol=https --remote",

    "// Testing": "",
    "test": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.4.5",
    "@cloudflare/workers-types": "^4.20240718.0",
    "typescript": "^5.5.2",
    "vite-tsconfig-paths": "^4.3.2",
    "vitest": "1.5.0",
    "wrangler": "^3.67.1"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.4"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "^4.19.0"
  },
  "overrides": {
    "@types/node": "20.8.3"
  }
}
