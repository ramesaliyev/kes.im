{
  "name": "kes.im",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "set-githooks": "node scripts/set-githooks",
    "ts-errcheck": "tsc --project tsconfig.errcheck.json",
    "gh-pre-commit": "npm run ts-errcheck && vitest run",
    "pre-deploy": "npm run ts-errcheck && node scripts/prepare",
    "deploy": "wrangler deploy --env prod",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.4.5",
    "@cloudflare/workers-types": "^4.20240718.0",
    "esbuild": "^0.23.0",
    "toml": "^3.0.0",
    "typescript": "^5.5.2",
    "uglify-js": "^3.19.0",
    "vite-tsconfig-paths": "^4.3.2",
    "vitest": "1.5.0",
    "wrangler": "^3.60.3"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.4"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "^4.19.0"
  },
  "overrides": {
    "@types/node": "20.8.3"
  }
}
